import { reviewSorting } from '../../../helpers/review-sorting';
import { module, test } from 'qunit';

module('Unit | Helper | review sorting');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reviewSorting(42);
  assert.ok(result);
});
