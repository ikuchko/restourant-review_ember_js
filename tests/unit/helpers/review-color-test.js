import { reviewColor } from '../../../helpers/review-color';
import { module, test } from 'qunit';

module('Unit | Helper | review color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reviewColor(42);
  assert.ok(result);
});
